// check iterate

function iteration(x) {
       count = 0;
    while (count < 5) {
        println(count);
        if (count == 3) {
            break;
        }
        count = count + 1;
	print("count is");
	println(count);

    for(i = 0; i < 5; i = i + 1) {
        print("i is ");
        println(i);
    }

    j = 0;
    while (j < x) {
        print("j is ");
        println(i);
        j = j + 1;
    }

 
}
}
iteration(6);


// check iterate
